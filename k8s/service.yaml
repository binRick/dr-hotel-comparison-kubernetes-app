apiVersion: v1
kind: Service
metadata:
  name: dr-api
  namespace: dr
  labels:
    app: dr-api
spec:
  # Internal only — reached by dr-web at http://dr-api:80
  type: ClusterIP
  selector:
    app: dr-api
  ports:
    - name: http
      port: 80
      targetPort: http
